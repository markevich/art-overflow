.form-user-comment
  = image_tag current_user.avatar, class:"border-radius-circle"
  .input-comment
    = simple_form_for Comment.new do |f|
      = f.input :text, as: :ckeditor, input_html: { ckeditor: {toolbar: 'Full'} }
      = f.hidden_field :commentable_type, value: commentable.class
      = f.hidden_field :commentable_id,   value: commentable.id
      = f.submit I18n.t('comment.create'), class: 'blue create-btn'
