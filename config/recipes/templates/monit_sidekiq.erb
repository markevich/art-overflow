check process sidekiq_<%= application %> with pidfile <%= sidekiq_pid %>
  start program = "/etc/init.d/sidekiq_<%= application %> start"
  stop program = "/etc/init.d/sidekiq_<%= application %> stop"
